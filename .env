# Leave this empty for local development so that the Vite proxy handles API requests.
# For Vercel production, set this in the Vercel Dashboard to your backend URL (e.g., https://api.yourdomain.com)
VITE_API_URL=
